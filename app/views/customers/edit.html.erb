<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing customer</h1>

  <%= render "form", customer: @customer %>

  <%= link_to "Show this customer", company_customer_path(company_id: @company.id, id: @customer.id), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
  <%= link_to "Back to customers", company_customers_path(@company), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
